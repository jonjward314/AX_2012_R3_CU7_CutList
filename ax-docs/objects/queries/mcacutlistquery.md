# Query: McaCutlistQuery

## Status
Referenced extensively (`querystr(McaCutlistQuery)` in contracts/DP/report metadata) but query definition is not separately exported in provided file set.

## What is confirmed
- Used as dynamic query parameter AOT query in SSRS metadata.
- Used as report DP default query.

## Unknowns
- Exact datasource tree, joins, ranges, and sort orders are **Not determinable from provided XPO/MD**.
